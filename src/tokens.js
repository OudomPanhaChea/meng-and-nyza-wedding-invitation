/* Shared design tokens used outside of CSS (e.g. inline GSAP styles,
   colour values that need to land in attribute style props). Keep this
   in sync with the Tailwind theme block in index.css. */
export const GOLD = "#b59410";
